// Runtime configuration for the app.
// scripts/deploy-app.sh regenerates this file from Terraform outputs, so the
// app is portable across Auth0 tenants without editing app.js. The values below
// are a working default for local use.
window.APP_CONFIG = {
  auth0Domain: "dev-k5xncag6gzsmst88.eu.auth0.com",
  auth0ClientId: "WSLWksd8eYgpXPshHPEdMlaT84NKrGSL",
};
